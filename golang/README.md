# Golang 🦿

## Layout

- `topic/` - language, stdlib, patterns
- `play/` - libraries and integrations (gin, grpc, redis, …)
- `course/` - exercise solutions

## New experiment

1. Folder:
   - `play/<slug>`, `course/<slug>`, `leetcode/<num>-<slug>`
   - `topic/<theme>/<slug>` when you need grouping
2. `go mod init golang/<path>`
