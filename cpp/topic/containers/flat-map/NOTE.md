# flat_map

`boost::container::flat_map` — ассоциативный контейнер на отсортированных contiguous-массивах
(обычно два `vector`), а не на дереве как `std::map`.

Нужен `libboost-container-dev` (header-only). Аналог в стандарте: `std::flat_map` (C++23).
